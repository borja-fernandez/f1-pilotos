CREATE SEQUENCE PILOTO_SQ
    START WITH 1
  INCREMENT BY 1
  MINVALUE 1;

CREATE SEQUENCE HABILIDAD_PILOTO_SQ
    START WITH 1
    INCREMENT BY 1
    MINVALUE 1;

CREATE SEQUENCE HABILIDAD_SQ
    START WITH 1
    INCREMENT BY 1
    MINVALUE 1;

CREATE TABLE PILOTO (
    SQ_PILOTO INT NOT NULL
        DEFAULT (NEXT VALUE FOR PILOTO_SQ),
    CODIGO_A3 NVARCHAR(3),
    NOMBRE NVARCHAR(255),
    FECHA_NACIMIENTO DATE,
        PRIMARY KEY (SQ_PILOTO)
    );

CREATE TABLE HABILIDAD (
    SQ_HABILIDAD INT NOT NULL
       DEFAULT (NEXT VALUE FOR HABILIDAD_SQ),
    CODIGO NVARCHAR(10),
    DESCRIPCION nvarchar(255),
        PRIMARY KEY (SQ_HABILIDAD)
);

CREATE TABLE HABILIDAD_PILOTO (
    SQ_HABILIDAD_PILOTO INT NOT NULL
      DEFAULT (NEXT VALUE FOR HABILIDAD_PILOTO_SQ),
    TIPO_REGISTRO NVARCHAR(12),
    FECHA_CREACION_REGISTRO TIMESTAMP,
    SQ_PILOTO INT NOT NULL,
    SQ_HABILIDAD INT NOT NULL,
    CANTIDAD INT DEFAULT 0,
        PRIMARY KEY (SQ_HABILIDAD_PILOTO),
        FOREIGN KEY (SQ_PILOTO) REFERENCES PILOTO(SQ_PILOTO),
        FOREIGN KEY (SQ_HABILIDAD) REFERENCES HABILIDAD(SQ_HABILIDAD)
);
